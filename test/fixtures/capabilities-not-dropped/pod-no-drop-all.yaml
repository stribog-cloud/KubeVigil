apiVersion: v1
kind: Pod
metadata:
  name: no-drop-all-pod
  namespace: default
spec:
  containers:
    - name: app
      image: nginx:1.25
      securityContext:
        capabilities:
          drop:
            - NET_RAW
