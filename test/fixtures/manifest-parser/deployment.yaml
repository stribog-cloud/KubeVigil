apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-deploy
  namespace: staging
spec:
  selector:
    matchLabels:
      app: test
  template:
    metadata:
      labels:
        app: test
    spec:
      containers:
        - name: web
          image: nginx:1.25
