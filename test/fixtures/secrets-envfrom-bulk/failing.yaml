apiVersion: v1
kind: Pod
metadata:
  name: envfrom-bulk-pod
  namespace: default
spec:
  containers:
    - name: app
      image: nginx:1.25
      envFrom:
        - secretRef:
            name: db-credentials
