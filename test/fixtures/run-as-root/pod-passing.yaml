apiVersion: v1
kind: Pod
metadata:
  name: nonroot-pod
  namespace: default
spec:
  containers:
    - name: app
      image: nginx:1.25
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
