apiVersion: v1
kind: Pod
metadata:
  name: multi-container-pod
  namespace: default
spec:
  containers:
    - name: secure-app
      image: nginx:1.25
      securityContext:
        privileged: false
    - name: privileged-sidecar
      image: redis:7
      securityContext:
        privileged: true
