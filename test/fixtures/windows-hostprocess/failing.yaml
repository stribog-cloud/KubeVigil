apiVersion: v1
kind: Pod
metadata:
  name: windows-hostprocess-pod
  namespace: default
spec:
  hostNetwork: true
  containers:
    - name: app
      image: mcr.microsoft.com/windows/nanoserver:ltsc2022
      securityContext:
        windowsOptions:
          hostProcess: true
