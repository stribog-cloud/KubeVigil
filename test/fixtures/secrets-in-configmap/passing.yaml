apiVersion: v1
kind: ConfigMap
metadata:
  name: normal-config
  namespace: default
data:
  log_level: "info"
  port: "8080"
  host: "localhost"
  debug: "false"
  max_replicas: "10"
  app_name: "my-application"
