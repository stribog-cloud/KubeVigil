apiVersion: v1
kind: Pod
metadata:
  name: var-log-pod
  namespace: default
spec:
  containers:
    - name: app
      image: nginx:1.25
      volumeMounts:
        - name: logs
          mountPath: /var/log
  volumes:
    - name: logs
      hostPath:
        path: /var/log
