apiVersion: v1
kind: Service
metadata:
  name: external-ip-service
  namespace: default
spec:
  type: ClusterIP
  externalIPs:
  - 10.0.0.1
  - 192.168.1.100
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: my-app
