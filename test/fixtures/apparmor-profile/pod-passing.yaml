apiVersion: v1
kind: Pod
metadata:
  name: apparmor-pod
  namespace: default
spec:
  containers:
    - name: app
      image: nginx:1.25
      securityContext:
        appArmorProfile:
          type: RuntimeDefault
