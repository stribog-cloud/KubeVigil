apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: escalation-verbs-role
  namespace: default
rules:
  - apiGroups: ["rbac.authorization.k8s.io"]
    resources: ["roles"]
    verbs: ["bind", "escalate"]
