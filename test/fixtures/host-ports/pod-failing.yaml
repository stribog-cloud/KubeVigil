apiVersion: v1
kind: Pod
metadata:
  name: host-port-pod
  namespace: default
spec:
  containers:
    - name: app
      image: nginx:1.25
      ports:
        - containerPort: 80
          hostPort: 8080
