apiVersion: v1
kind: Pod
metadata:
  name: readonly-rootfs-pod
  namespace: default
spec:
  containers:
    - name: app
      image: nginx:1.25
      securityContext:
        readOnlyRootFilesystem: true
