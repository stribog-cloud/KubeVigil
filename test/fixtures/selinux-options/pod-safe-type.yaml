apiVersion: v1
kind: Pod
metadata:
  name: selinux-safe-pod
  namespace: default
spec:
  containers:
    - name: app
      image: nginx:1.25
      securityContext:
        seLinuxOptions:
          type: container_t
