apiVersion: v1
kind: Pod
metadata:
  name: no-ephemeral-storage-request-pod
  namespace: default
spec:
  containers:
    - name: app
      image: nginx:1.25
      resources:
        limits:
          ephemeral-storage: "2Gi"
