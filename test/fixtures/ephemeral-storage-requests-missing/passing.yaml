apiVersion: v1
kind: Pod
metadata:
  name: ephemeral-storage-request-set-pod
  namespace: default
spec:
  containers:
    - name: app
      image: nginx:1.25
      resources:
        requests:
          ephemeral-storage: "1Gi"
        limits:
          ephemeral-storage: "2Gi"
