apiVersion: apps/v1
kind: Deployment
metadata:
  name: host-pid-deploy
  namespace: default
spec:
  selector:
    matchLabels:
      app: test
  template:
    metadata:
      labels:
        app: test
    spec:
      hostPID: true
      containers:
        - name: app
          image: nginx:1.25
