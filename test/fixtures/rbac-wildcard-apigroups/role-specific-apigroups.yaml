apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: specific-apigroups-role
  namespace: default
rules:
  - apiGroups: ["", "apps"]
    resources: ["pods", "deployments"]
    verbs: ["get", "list", "watch"]
