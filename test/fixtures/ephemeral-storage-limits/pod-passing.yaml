apiVersion: v1
kind: Pod
metadata:
  name: ephemeral-storage-set-pod
  namespace: default
spec:
  containers:
    - name: app
      image: nginx:1.25
      resources:
        limits:
          ephemeral-storage: "1Gi"
