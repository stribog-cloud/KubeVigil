apiVersion: apps/v1
kind: Deployment
metadata:
  name: host-ipc-deploy
  namespace: default
spec:
  selector:
    matchLabels:
      app: test
  template:
    metadata:
      labels:
        app: test
    spec:
      hostIPC: true
      containers:
        - name: app
          image: nginx:1.25
