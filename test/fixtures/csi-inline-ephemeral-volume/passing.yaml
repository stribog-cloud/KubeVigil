apiVersion: v1
kind: Pod
metadata:
  name: pvc-consumer
  namespace: default
spec:
  containers:
    - name: app
      image: nginx:1.25
      volumeMounts:
        - name: data
          mountPath: /mnt/data
  volumes:
    - name: data
      persistentVolumeClaim:
        claimName: reviewed-pvc
