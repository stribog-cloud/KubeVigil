apiVersion: v1
kind: Pod
metadata:
  name: projected-token-pod
  namespace: default
spec:
  containers:
    - name: app
      image: nginx:1.25
  volumes:
    - name: sa-token
      projected:
        sources:
          - serviceAccountToken:
              expirationSeconds: 3600
              audience: api
              path: token
