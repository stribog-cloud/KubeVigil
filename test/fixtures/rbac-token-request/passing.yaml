apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: token-request-passing-role
  namespace: default
rules:
  - apiGroups: [""]
    resources: ["serviceaccounts/token"]
    resourceNames: ["my-app-sa"]
    verbs: ["create"]
