apiVersion: batch/v1
kind: Job
metadata:
  name: batch-processor
  namespace: default
spec:
  activeDeadlineSeconds: 3600
  template:
    spec:
      containers:
        - name: processor
          image: batch-processor:v1
      restartPolicy: Never
