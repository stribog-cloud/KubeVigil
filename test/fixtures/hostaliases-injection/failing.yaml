apiVersion: v1
kind: Pod
metadata:
  name: hostaliases-injection-pod
  namespace: default
spec:
  hostAliases:
    - ip: "10.0.0.99"
      hostnames: ["kubernetes.default.svc"]
  containers:
    - name: app
      image: nginx:1.25
