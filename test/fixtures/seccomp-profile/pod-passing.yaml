apiVersion: v1
kind: Pod
metadata:
  name: seccomp-pod
  namespace: default
spec:
  containers:
    - name: app
      image: nginx:1.25
      securityContext:
        seccompProfile:
          type: RuntimeDefault
