apiVersion: v1
kind: Pod
metadata:
  name: pod-level-seccomp
  namespace: default
spec:
  securityContext:
    seccompProfile:
      type: RuntimeDefault
  containers:
    - name: app
      image: nginx:1.25
