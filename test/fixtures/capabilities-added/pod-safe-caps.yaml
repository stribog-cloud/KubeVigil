apiVersion: v1
kind: Pod
metadata:
  name: safe-caps-pod
  namespace: default
spec:
  containers:
    - name: app
      image: nginx:1.25
      securityContext:
        capabilities:
          drop:
            - ALL
